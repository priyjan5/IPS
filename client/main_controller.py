# This represents the controller class which controls the whole flow
import config
class MainController(object):
	def __init__(self, configFile):
		self.config = config.Config(configFile)
		
	def start(configFile):
		print 'MainController.start(): METHOD NOT IMPLEMENTED'
		return
	
